// const foodService = require('foodService')

class Meal {

  constructor(name) {
    this.name = name;
    this.id = id;
    this.base = 0;
    this.foods = [];
  };

  function updateFoods() {
    // service to call foods for meal
  };

  function total() {
    // add up all foods calories
  };

  function remaining() {
    // subtract total from base
  };
};

module.exports = Meal;